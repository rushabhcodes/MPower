import 'package:flutter/material.dart';

class UserDataCollection extends StatelessWidget {
  const UserDataCollection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}